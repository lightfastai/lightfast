/**
 * Stub for the "server-only" Next.js package.
 *
 * In Next.js builds, this package throws an error when imported from client-side
 * code. In the integration test environment (Node.js/Vitest), we stub it as a
 * no-op so that server-side modules can be imported without Next.js infrastructure.
 */
export {};
