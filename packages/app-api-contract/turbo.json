{
  "extends": ["//"],
  "tags": ["packages"],
  "tasks": {
    "generate:openapi": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "scripts/**"],
      "outputs": ["openapi.json"]
    }
  }
}
