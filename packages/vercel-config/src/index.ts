/**
 * Vercel configuration utilities
 *
 * NOTE: URL configuration has been moved to app-specific related-projects.ts files
 * Each app should use Vercel's related projects feature via withRelatedProject()
 */
export * from "./project-config";