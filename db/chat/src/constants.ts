/**
 * Shared constants for the Lightfast chat domain.
 * Keep this module lightweight so it can be safely imported by client bundles
 * without dragging in the full database schema.
 */
export const DEFAULT_SESSION_TITLE = "New Session";
