/create_plan @thoughts/shared/research/2026-04-07-clerk-team-member-github-org-migration.md

---

makes sense.

---

question wouldnt this technically change the org create flow too. since we're linking single github org with a single lightfast org.

---

hmm, but im just thinking that it makes more sense for the org name in lightfast to be their org name in github tho.

---

i think for day 1 to 100 we expect every user to have a github org. if not, not part of lightfast. simple and straight foraward.

---

i think the flow should be more straightforward. so it's connect github org -> shows lightfast of orgs -> click org X -> creates org. so not automated. like a proper flow.

---

rethink this. orgIg nullable + create pending are both imo bad solutions. nullable orgId in gatewayInstallation will cause code smells all throughout our app.

---

hmm, can we check what Clerk's getUserOauthAccessTOken provides. create a route or somehting @apps/app/ and ill go check

---

hmm, can we check what Clerk's getUserOauthAccessTOken provides. create a route  @apps/app/ that uses the clerkClient to output the getUserOauthAccessToken.

this should allow us to check what gets returned with this token.

---

hmm, can we check what Clerk's getUserOauthAccessTOken provides. create a route  @apps/app/src/app/api/  that uses the clerkClient to output the getUserOauthAccessToken.

this should allow us to check what gets returned with this token.

---

hmm, we want to figure out what Clerk's getUserOauthAccessTOken provides. create a route  @apps/app/src/app/api/  that uses the clerkClient to output the getUserOauthAccessToken.

this should allow us to check what gets returned with this token.

do not run research. just create this route for me.