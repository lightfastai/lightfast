{
  "name": "lightfast-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Lightfast experimental monorepo",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "dev:web": "turbo run dev --filter=@lightfast/web",
    "dev:mastra": "turbo run dev:mastra --filter=@lightfast/web",
    "build:web": "turbo run build --filter=@lightfast/web",
    "build:mastra": "turbo run build:mastra --filter=@lightfast/web"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.1",
    "turbo": "^2.5.5",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@clerk/shared",
      "@tailwindcss/oxide",
      "@vercel/speed-insights",
      "better-sqlite3",
      "esbuild",
      "protobufjs",
      "sharp"
    ]
  }
}