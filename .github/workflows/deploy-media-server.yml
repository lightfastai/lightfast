# name: Deploy Media Server
# on:
#   push:
#     branches:
#       - main
#     paths:
#       - "apps/media-server/**"
#       - "packages/ai/**"
#       - "packages/lib/**"
#       - "vendor/db/**"
#       - ".github/workflows/deploy-media-server.yml"
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     env:
#       TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
#       TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
#       CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#       CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 20

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: 10.5.2

#       - name: Get pnpm store directory
#         id: pnpm-cache
#         run: |
#           echo "pnpm_cache_dir=$(pnpm store path)" >> $GITHUB_OUTPUT

#       - name: Setup pnpm cache
#         uses: actions/cache@v3
#         with:
#           path: ${{ steps.pnpm-cache.outputs.pnpm_cache_dir }}
#           key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
#           restore-keys: |
#             ${{ runner.os }}-pnpm-store-

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Build
#         run: pnpm turbo build --filter=@lightfast/media-server

#       - name: Set Wrangler Environment Variables
#         run: |
#           cd apps/media-server
#           echo "R2_ACCESS_KEY_ID = \"${{ secrets.R2_ACCESS_KEY_ID }}\"" >> .dev.vars
#           echo "R2_SECRET_ACCESS_KEY = \"${{ secrets.R2_SECRET_ACCESS_KEY }}\"" >> .dev.vars
#           echo "R2_ACCOUNT_ID = \"${{ secrets.R2_ACCOUNT_ID }}\"" >> .dev.vars
#           echo "R2_BUCKET_NAME = \"${{ secrets.R2_BUCKET_NAME }}\"" >> .dev.vars
#           echo "SUPABASE_URL = \"${{ secrets.SUPABASE_URL }}\"" >> .dev.vars
#           echo "SUPABASE_ANON_KEY = \"${{ secrets.SUPABASE_ANON_KEY }}\"" >> .dev.vars
#           echo "SUPABASE_PROJECT_ID = \"${{ secrets.SUPABASE_PROJECT_ID }}\"" >> .dev.vars
#           echo "OPENAI_API_KEY = \"${{ secrets.OPENAI_API_KEY }}\"" >> .dev.vars
#           echo "FAL_KEY = \"${{ secrets.FAL_KEY }}\"" >> .dev.vars
#           echo "BASE_URL = \"${{ secrets.WORKER_BASE_URL }}\"" >> .dev.vars
#           echo "NODE_ENV = \"production\"" >> .dev.vars

#       - name: Deploy
#         run: |
#           cd apps/media-server
#           chmod +x deploy.sh
#           ./deploy.sh
