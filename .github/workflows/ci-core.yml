name: core-public-api-ci
run-name: core-public-api-ci / ${{ github.event_name }} / ${{ github.head_ref || github.ref_name }}

# Runs on PRs touching the public API/SDK/MCP surface that need the
# focused core public API lint/typecheck/test suite.
on:
  push:
    branches: [main]
    paths:
      - 'api/app/**'
      - 'apps/mcp/**'
      - 'core/**'
      - 'packages/api-contract/**'
      - 'packages/mcp-tools/**'
      - 'vendor/mcp/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/ci-core.yml'
  pull_request:
    branches: [main]
    paths:
      - 'api/app/**'
      - 'apps/mcp/**'
      - 'core/**'
      - 'packages/api-contract/**'
      - 'packages/mcp-tools/**'
      - 'vendor/mcp/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/ci-core.yml'

# Cancel previous runs on new pushes to the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  core-public-api-lint-typecheck-test:
    name: core-public-api-lint-typecheck-test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
    steps:
      - name: checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - name: setup-pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4.4.0

      - name: setup-node-22
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: 'pnpm'

      - name: install-dependencies
        run: pnpm install --frozen-lockfile

      - name: lint-biome
        run: pnpm turbo run //#check --summarize

      - name: typecheck-public-surface
        env:
          SKIP_ENV_VALIDATION: "true"
        run: pnpm turbo run typecheck --filter=@repo/api-contract --filter=@api/app --filter=lightfast --filter=@lightfastai/cli --filter=@lightfastai/mcp --filter=@lightfast/mcp-local --filter=@repo/mcp-tools --filter=@vendor/mcp --continue --summarize

      - name: test-public-surface
        env:
          SKIP_ENV_VALIDATION: "true"
        run: pnpm turbo run test --filter=@repo/api-contract --filter=@api/app --filter=lightfast --filter=@lightfastai/cli --filter=@lightfastai/mcp --filter=@lightfast/mcp-local --filter=@repo/mcp-tools --filter=@vendor/mcp --continue --summarize

      - name: turbo-cache-report
        if: always()
        uses: ./.github/actions/turbo-summary
        with:
          title: 'Turbo cache report - core-public-api-ci / core-public-api-lint-typecheck-test'
          artifact-name: turbo-cache-core-public-api-ci-lint-typecheck-test
