name: Release desktop

on:
  push:
    tags:
      - '@lightfast/desktop@*'

concurrency:
  group: desktop-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare draft release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
      tag: ${{ steps.ver.outputs.tag }}
      prerelease: ${{ steps.ver.outputs.prerelease }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: ver
        run: |
          tag="${{ github.ref_name }}"
          version="${tag#@lightfast/desktop@}"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          # Any hyphen-suffix (-beta.N, -rc.N, -alpha.N) is a semver prerelease.
          if [[ "$version" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create draft release if missing
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "${{ steps.ver.outputs.tag }}" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            echo "Release already exists, reusing."
          else
            prev=$(git tag -l '@lightfast/desktop@*' --sort=-v:refname | grep -v "${{ steps.ver.outputs.tag }}" | head -n1 || true)
            if [ -n "$prev" ]; then
              notes_arg=(--generate-notes --notes-start-tag "$prev")
            else
              # First desktop tag: skip --generate-notes to avoid pulling every commit since
              # repo init onto the public release page. Hand-write a one-liner; subsequent
              # tags get autogenerated notes via $prev.
              notes_arg=(--notes "Initial Lightfast desktop beta. See [README](https://github.com/lightfastai/lightfast/blob/main/apps/desktop/README.md) for install instructions.")
            fi
            # PublisherGithub never updates the prerelease flag on existing
            # releases (only sets it inside createRelease, never updateRelease).
            # Set --prerelease at draft creation; otherwise the publisher will
            # find the existing draft and leave prerelease:false intact.
            prerelease_flag=()
            if [ "${{ steps.ver.outputs.prerelease }}" = "true" ]; then
              prerelease_flag=(--prerelease)
            fi
            gh release create "${{ steps.ver.outputs.tag }}" \
              --repo "${{ github.repository }}" \
              --draft \
              "${prerelease_flag[@]}" \
              --title "lightfast@${{ steps.ver.outputs.version }}" \
              "${notes_arg[@]}"
          fi

  build_macos:
    name: Build macOS ${{ matrix.arch }}
    needs: prepare
    runs-on: macos-14
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x64]
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
      APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      SENTRY_ORG: ${{ vars.SENTRY_ORG }}
      SENTRY_PROJECT: ${{ vars.SENTRY_PROJECT }}
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Stamp version, feed URL, signing mode, prerelease env
        working-directory: apps/desktop
        run: |
          npm version "${{ needs.prepare.outputs.version }}" --no-git-tag-version --allow-same-version
          npm pkg set buildFlavor=prod
          npm pkg set buildNumber="$GITHUB_RUN_NUMBER"
          npm pkg set sparkleFeedUrl='https://github.com/${{ github.repository }}/releases/latest/download/latest-mac-${arch}.json'
          if [ -n "$APPLE_SIGNING_IDENTITY" ]; then
            npm pkg set signingMode=developer-id
          else
            npm pkg set signingMode=ad-hoc
          fi
          # forge.config.ts reads LIGHTFAST_DESKTOP_RELEASE_PRERELEASE for the
          # publisher's createRelease fallback path. The prepare job already
          # set --prerelease at draft creation (the load-bearing path); this
          # is belt-and-suspenders.
          echo "LIGHTFAST_DESKTOP_RELEASE_PRERELEASE=${{ needs.prepare.outputs.prerelease }}" >> "$GITHUB_ENV"

      - name: Import Apple signing certificate
        if: env.APPLE_SIGNING_IDENTITY != ''
        env:
          APPLE_CERT_BASE64: ${{ secrets.APPLE_CERT_BASE64 }}
          APPLE_CERT_PASSWORD: ${{ secrets.APPLE_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          echo "$APPLE_CERT_BASE64" | base64 -d > /tmp/cert.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import /tmp/cert.p12 -k build.keychain -P "$APPLE_CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm /tmp/cert.p12

      - name: Write notarize API key
        if: env.APPLE_SIGNING_IDENTITY != ''
        env:
          APPLE_API_KEY_CONTENT: ${{ secrets.APPLE_API_KEY_CONTENT }}
          APPLE_API_KEY_ID_SECRET: ${{ secrets.APPLE_API_KEY_ID }}
        run: |
          mkdir -p "$HOME/.private_keys"
          key_file="$HOME/.private_keys/AuthKey_${APPLE_API_KEY_ID_SECRET}.p8"
          printf '%s' "$APPLE_API_KEY_CONTENT" > "$key_file"
          echo "APPLE_API_KEY=$key_file" >> "$GITHUB_ENV"

      - name: Publish (forge)
        working-directory: apps/desktop
        run: pnpm exec electron-forge publish --arch=${{ matrix.arch }} --platform=darwin

      - name: Upload source maps to Sentry
        working-directory: apps/desktop
        run: pnpm sourcemaps:upload

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            apps/desktop/out/make/**/*.zip
            apps/desktop/out/make/**/*.dmg

  build_linux:
    name: Build Linux ${{ matrix.arch }}
    needs: prepare
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: ubuntu-22.04
          - arch: arm64
            runner: ubuntu-22.04-arm
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # Intentionally no Sentry env vars: sourcemap upload runs on the mac
      # leg only. Sentry's debug-id system dedupes server-side and the
      # renderer/main JS source is identical across platforms, so a second
      # upload would be duplicated work, not added coverage.
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Linux maker prerequisites
        run: |
          sudo apt-get update && sudo apt-get install -y rpm fakeroot
          # Fail fast if the runner image (x64 or arm) is missing any tool
          which dpkg-deb fakeroot rpm rpmbuild

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Stamp version, buildFlavor, buildNumber, signingMode, prerelease env
        working-directory: apps/desktop
        run: |
          npm version "${{ needs.prepare.outputs.version }}" --no-git-tag-version --allow-same-version
          npm pkg set buildFlavor=prod
          npm pkg set buildNumber="$GITHUB_RUN_NUMBER"
          # `signingMode=ad-hoc` keeps build-info shape platform-uniform.
          # `updater.ts:32-42` returns null on Linux today, so this field is
          # not yet load-bearing. The future Linux Sparkle PR will read it as
          # the safe-default guard (same pattern as the mac path). Delete this
          # line if/when Linux Sparkle introduces a Linux-specific signing scheme.
          npm pkg set signingMode=ad-hoc
          echo "LIGHTFAST_DESKTOP_RELEASE_PRERELEASE=${{ needs.prepare.outputs.prerelease }}" >> "$GITHUB_ENV"

      - name: Publish (forge)
        working-directory: apps/desktop
        run: pnpm exec electron-forge publish --arch=${{ matrix.arch }} --platform=linux

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            apps/desktop/out/make/**/*.deb
            apps/desktop/out/make/**/*.rpm

  finalize:
    name: Finalize release
    needs: [prepare, build_macos, build_linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Squirrel.Mac update feed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          RELEASE_VERSION: ${{ needs.prepare.outputs.version }}
        run: node apps/desktop/scripts/generate-update-feed.mjs

      - name: Publish release (undraft)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release edit "${{ needs.prepare.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --draft=false
