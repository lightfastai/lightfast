name: "Lightfast CodeQL config"

# Skip dev-only and non-source paths from CodeQL analysis.
# CodeQL already excludes dependency and build-output directories by default.
# Only list repository tooling that is not user-facing source.
paths-ignore:
  - scripts/**
  - "**/__generated__/**"
  - "**/generated/**"
