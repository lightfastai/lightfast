import { AboutWave } from "~/components/landing/about-wave";

export default function AboutPage() {
  return (
    <div className="manifesto-page relative bg-background min-h-screen flex items-center justify-center p-8">
      <AboutWave />
    </div>
  );
}
