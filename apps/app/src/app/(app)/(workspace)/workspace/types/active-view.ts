export enum ActiveView {
  WORKSPACE = "WORKSPACE", // Includes canvas, node manipulation, and inspector
  COMMAND = "COMMAND", // Command palette
  FILE_MENU = "FILE_MENU", // Top breadcrumb menu
  NONE = "NONE",
}
