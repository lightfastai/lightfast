{
  "name": "@lightfast/backfill",
  "license": "FSL-1.1-Apache-2.0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsup && pnpm with-env node dist/src/env.js",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "NODE_ENV=development pnpm with-env srvx --prod --import tsx -p 4109 src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ./.vercel/.env.development.local --"
  },
  "dependencies": {
    "@repo/console-providers": "workspace:*",
    "@repo/console-validation": "workspace:*",
    "@repo/gateway-service-clients": "workspace:*",
    "@repo/inngest": "workspace:*",
    "@repo/lib": "workspace:*",
    "@t3-oss/env-core": "catalog:",
    "@vendor/inngest": "workspace:*",
    "@vendor/observability": "workspace:*",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "fast-check": "catalog:",
    "srvx": "^0.11.8",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
