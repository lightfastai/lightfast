// Alias to maintain backwards compatibility with existing scripts expecting tool-call-repair.eval.ts
// Reuse the more descriptive built-in repair evaluation without duplicating logic.
import "./built-in-repair.eval";
