import { ModelConfigSchema } from "../schemas";

/**
 * Deprecated AI Models
 *
 * These models are no longer recommended but remain functional
 * for backward compatibility with existing conversations.
 */
export const DEPRECATED_MODELS = {
	// Add deprecated models here when needed
} as const;