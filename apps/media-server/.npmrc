# Prevent EMFILE errors by limiting concurrency
jobs=1
network-concurrency=1
child-concurrency=1
# Disable fund messages to reduce output noise
fund=false
# Reduce file ops that can lead to EMFILE errors
prefer-offline=true
# Other helpful settings
cache-min=3600
loglevel=warn 